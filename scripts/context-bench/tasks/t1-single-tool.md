# T1 — 单工具小任务（基线）

目的：不触发 tool_search，全程只用 direct 工具，作为所有对比的基线臂。
预期行为：loaded 始终为 0，toolset 不变，cache.read 应稳定爬升。

## Turn 1

在当前目录创建文件 `notes/weekly-plan.md`，内容为一页本周工作计划：三个小标题（产品研发、内容运营、数据复盘），每个小标题下三条具体待办。写完后不用汇报细节，确认已创建即可。

## Turn 2

读一下 `notes/weekly-plan.md`，在文件末尾追加一节「风险与对策」，写两条可能的风险和对应的缓解办法。保持原有内容不变。
